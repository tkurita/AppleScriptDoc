{
    IBClasses = (
        {
            ACTIONS = {makeDonation = id; popUpRecents = id; }; 
            CLASS = AppController; 
            LANGUAGE = ObjC; 
            OUTLETS = {recentScriptsButton = id; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}